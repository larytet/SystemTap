set test "optionalprobe"
eval spawn stap [stap_get_board_args] -p2 -w $srcdir/$subdir/$test.stp
expect {
    -timeout 300
    -re "# probes\r\n" { exp_continue }
    -re "^begin" { pass $test }
    eof { fail $test }
    timeout { fail "$test unexpected timeout" }
}
catch {close}; catch {wait}
